{
  "name": "github.com~bash-origin~bash.origin.profile",
  "version": "0.0.0",
  "dependencies": {
    "bash.origin": "^0.1.60",
    "bash.origin.test": "^0.1.31",
    "pio.profile": "^0.1.2"
  },
  "scripts": {
    "test": "BO_ROOT_SCRIPT_PATH=$(pwd)/node_modules/bash.origin/bash.origin bash.origin.test Examples"
  }
}
